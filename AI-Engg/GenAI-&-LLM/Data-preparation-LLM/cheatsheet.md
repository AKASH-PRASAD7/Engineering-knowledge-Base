# CheatSheet

| Package/Method                      | Description                                                                                                        | Code Example                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| ----------------------------------- | ------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| NLTK                                | NLTK is a library used in natural language processing (NLP) for tasks such as tokenization and text processing.    | `1. import nltk 2. nltk.download("punkt") 3. from nltk.tokenize import word_tokenize 4. text = "Unicorns are real. I saw a unicorn yesterday. I couldn't see it today." 5. token = word_tokenize(text) 6. print(token)`                                                                                                                                                                                                                                                                                                                                                                                                                    |
| spaCy                               | spaCy is an open-source library used in NLP. It provides tools for tasks such as tokenization and word embeddings. | `1. import spacy 2. text = "Unicorns are real. I saw a unicorn yesterday. I couldn't see it today." 3. nlp = spacy.load("en_core_web_sm") 4. doc = nlp(text) 5. token_list = [token.text for token in doc] 6. print("Tokens:", token_list)`                                                                                                                                                                                                                                                                                                                                                                                                |
| BertTokenizer                       | BertTokenizer is a subword-based tokenizer that uses the WordPiece algorithm.                                      | `1. from transformers import BertTokenizer 2. tokenizer = BertTokenizer.from_pretrained("bert-base-uncased") 3. tokenizer.tokenize("IBM taught me tokenization.")`                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| XLNetTokenizer                      | XLNetTokenizer tokenizes text using Unigram and SentencePiece algorithms.                                          | `1. from transformers import XLNetTokenizer 2. tokenizer = XLNetTokenizer.from_pretrained("xlnet-base-cased") 3. tokenizer.tokenize("IBM taught me tokenization.")`                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| torchtext                           | The torchtext library is part of the PyTorch ecosystem and provides tools and functionalities required for NLP.    | `1. from torchtext.vocab import build_vocab_from_iterator 2. from torchtext.data.utils import get_tokenizer 3. dataset = [ 4. (1,"Introduction to NLP"), 5. (2,"Basics of PyTorch"), 6. (1,"NLP Techniques for Text Classification") 7. ] 8. tokenizer = get_tokenizer("basic_english") 9. tokenizer(dataset[0][1]) 10. def yield_tokens(data_iter): 11. for _,text in data_iter: 12. yield tokenizer(text) 13. my_iterator = yield_tokens(dataset) 14. next(my_iterator) 15. vocab = build_vocab_from_iterator( 16. yield_tokens(dataset), 17. specials=["<unk>"] 18. ) 19. vocab.set_default_index(vocab["<unk>"]) 20. vocab.get_stoi()` |
| vocab                               | The vocab object maps tokens to indices.                                                                           | `1. def get_tokenized_sentence_and_indices(iterator): 2. tokenized_sentence = next(iterator) 3. token_indices = [vocab[token] for token in tokenized_sentence] 4. return tokenized_sentence, token_indices 5. tokenized_sentence, token_indices = \ 6. get_tokenized_sentence_and_indices(my_iterator) 7. print("Tokenized Sentence:", tokenized_sentence) 8. print("Token Indices:", token_indices)`                                                                                                                                                                                                                                      |
| Special tokens: `<bos>` and `<eos>` | `<bos>` denotes the beginning of a sequence and `<eos>` denotes the end of a sequence.                             | `1. tokenizer_en = get_tokenizer('spacy', language='en_core_web_sm') 2. tokens = [] 3. max_length = 0 4. for line in lines: 5. tokenized_line = tokenizer_en(line) 6. tokenized_line = ['<bos>'] + tokenized_line + ['<eos>'] 7. tokens.append(tokenized_line) 8. max_length = max(max_length, len(tokenized_line))`                                                                                                                                                                                                                                                                                                                       |
| Special token: `<pad>`              | `<pad>` is used to make all tokenized sequences the same length.                                                   | `1. for i in range(len(tokens)): 2. tokens[i] = tokens[i] + ['<pad>'] * (max_length - len(tokens[i]))`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| Dataset class                       | The Dataset class enables accessing and retrieving individual samples from a dataset.                              | `1. from torch.utils.data import Dataset 2. sentences = ["Sentence one", "Sentence two"] 3. class CustomDataset(Dataset): 4. def __init__(self, sentences): 5. self.sentences = sentences 6. def __len__(self): 7. return len(self.sentences) 8. def __getitem__(self, idx): 9. return self.sentences[idx] 10. dataset = CustomDataset(sentences) 11. dataset[0]`                                                                                                                                                                                                                                                                          |
| DataLoader class                    | DataLoader enables efficient loading and batching of datasets for model training.                                  | `1. from torch.utils.data import DataLoader 2. custom_dataset = CustomDataset(sentences) 3. batch_size = 2 4. dataloader = DataLoader( 5. custom_dataset, 6. batch_size=batch_size, 7. shuffle=True 8. ) 9. for batch in dataloader: 10. print(batch)`                                                                                                                                                                                                                                                                                                                                                                                     |
| Custom collate function             | A custom collate function defines how samples are batched together.                                                | `1. def collate_fn(batch): 2. tensor_batch = [] 3. for sample in batch: 4. tokens = tokenizer(sample) 5. tensor_batch.append( 6. torch.tensor([vocab[token] for token in tokens]) 7. ) 8. padded_batch = pad_sequence( 9. tensor_batch, 10. batch_first=True 11. ) 12. return padded_batch 13. dataloader = DataLoader( 14. custom_dataset, 15. batch_size=batch_size, 16. shuffle=True, 17. collate_fn=collate_fn 18. )`                                                                                                                                                                                                                  |
